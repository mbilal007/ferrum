/** Browse command placeholder — implemented in Phase 2 */
export async function browseCommand(_url: string): Promise<void> {
  console.error('Browse command requires Phase 2 (CDP). Coming soon.');
  console.error('For now, use: ferrum render <image>');
  process.exit(1);
}
